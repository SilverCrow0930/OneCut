import { Image, Video } from 'lucide-react';
import { AssetType } from '../types/assets';

export const ASSET_TABS: AssetType[] = [
    { id: 'video', label: 'Video', icon: Video },
    { id: 'image', label: 'Image', icon: Image },
]; 